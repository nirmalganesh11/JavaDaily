package LinkedList;

public interface NirmalList {
   node findNode(int data);
   node backTrack(node head);
   node frontTrack (node head);
   node createCircle(node head);
   node display(node head);
}
